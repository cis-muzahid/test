---
name: Hellow world workflow
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: null
jobs:
  hi:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: hi world
        run: echo "hi world"
        shell: bash
